// Criando um programa de contagem regressiva. Peça um número e conte deste número até 0, usando um loop while no console do navegador.


alert('Veja o console para conferir a contagem regressiva');

let contador = prompt('Digite um número para iniciar a contagem regressiva');

while (contador >= 0 ) {
    console.log(contador);
    contador--;
}

